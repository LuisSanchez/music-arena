# Leave empty for local dev (Vite proxies /api → http://127.0.0.1:8000).
# On Vercel, set this to your deployed API origin (no trailing slash).
# VITE_API_URL=https://your-clash-api.fly.dev
VITE_API_URL=
